healthchecks:
  inherit: base
  env:
    HEALTHCHECKS_UUID: "your-healthchecks-uuid-here"

  backup:
    source: 
      - {{ .Env.HOME }}/compose/healthchecks
      - {{ .Env.HOME }}/appdata/healthchecks

  retention: 
{{ template "retention-iron" . }}

  forget:
{{ template "retention-iron" . }}